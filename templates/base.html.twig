<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}NBA Project{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🏀</text></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}
        <style>
            body {
                background-color: #ffffff;
                margin: 0;
                padding: 0;
                min-height: 100vh;
            }

            .navbar {
                background-color: #212529 !important;
                border-bottom: none !important;
                padding: 15px 20px;
            }

            .navbar-brand {
                font-weight: bold;
                color: white !important;
                font-size: 1.3rem;
                display: flex;
                align-items: center;
                gap: 8px;
            }

            .btn-nav-custom {
                border-radius: 20px;
                padding: 6px 20px;
                font-weight: 600;
                font-size: 0.9rem;
                text-decoration: none;
                margin-left: 10px;
                transition: transform 0.2s;
            }
            .btn-nav-custom:hover {
                transform: scale(1.05);
            }

            .btn-login-nav {
                background-color: #f89c0c;
                color: white;
                border: 1px solid #f89c0c;
            }

            .btn-register-nav {
                background-color: #f89c0c;
                color: white;
                border: 1px solid #f89c0c;
            }
        </style>
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <span style="font-size: 1.5rem;">🏀</span> NBA
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
                {% if app.user %}
                    <li class="nav-item">
                        <span class="nav-link text-white">Hola, {{ app.user.userIdentifier }}</span>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="btn btn-danger btn-sm ms-3 rounded-pill px-3">Cerrar Sesión</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="btn-nav-custom btn-login-nav">INICIAR SESIÓN</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_register') }}" class="btn-nav-custom btn-register-nav">REGISTRARSE</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

{% block body %}{% endblock %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
