{% extends 'base.html.twig' %}

{% block title %}Acceso Denegado{% endblock %}

{% block body %}
    <style>
        /* Estilos específicos para la página de error */
        .error-container {
            min-height: calc(100vh - 76px);
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #212529; /* Fondo oscuro */
            padding: 20px;
        }

        .error-card {
            background: white;
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            border-top: 8px solid #ce1141; /* Rojo NBA */
        }

        .icon-error {
            font-size: 5rem;
            margin-bottom: 20px;
            display: block;
        }

        .error-title {
            color: #ce1141;
            font-weight: 900;
            text-transform: uppercase;
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .error-desc {
            color: #555;
            font-size: 1.1rem;
            margin-bottom: 30px;
        }

        .btn-nba {
            background-color: #1d428a;
            color: white;
            padding: 12px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            transition: transform 0.2s;
            display: inline-block;
        }

        .btn-nba:hover {
            background-color: #143066;
            color: white;
            transform: scale(1.05);
        }
    </style>

    <div class="error-container">
        <div class="error-card">
            <span class="icon-error">🚫👮‍♂️</span>
            <h1 class="error-title">FALTA TÉCNICA</h1>
            <p class="error-desc">
                <strong>Error 403: Acceso Prohibido</strong><br><br>
                Lo siento, no tienes permisos de <strong>Administrador</strong> para entrar en esta zona.
            </p>

            <a href="{{ path('app_home') }}" class="btn-nba">
                🏀 VOLVER A LA CANCHA
            </a>
        </div>
    </div>
{% endblock %}
